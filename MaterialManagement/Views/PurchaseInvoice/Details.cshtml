@* @model MaterialManagement.BLL.ModelVM.Invoice.PurchaseInvoiceViewModel
@using MaterialManagement.DAL.Entities

@{
    ViewData["Title"] = $"تفاصيل فاتورة الشراء #{Model.InvoiceNumber}";
    var payments = ViewBag.Payments as IEnumerable<SupplierPayment>;
    var totalPayments = payments?.Sum(p => p.Amount) ?? 0;
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1><i class="bi bi-receipt-cutoff"></i> تفاصيل فاتورة الشراء: <code>@Model.InvoiceNumber</code></h1>
    <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-list-ul"></i> العودة للفواتير</a>
</div>
<hr />

<!-- Invoice Summary Card -->
<div class="card shadow-sm mb-4">
    <div class="card-header bg-light">
        <h5 class="mb-0">ملخص الفاتورة</h5>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                <dl class="row">
                    <dt class="col-sm-4">المورد</dt>
                    <dd class="col-sm-8"><a asp-controller="Supplier" asp-action="Details" asp-route-id="@Model.SupplierId">@Model.SupplierName</a></dd>

                    <dt class="col-sm-4">تاريخ الفاتورة</dt>
                    <dd class="col-sm-8">@Model.InvoiceDate.ToString("D")</dd>
                </dl>
            </div>
            <div class="col-md-6">
                <dl class="row">
                    <dt class="col-sm-4">الإجمالي</dt>
                    <dd class="col-sm-8 fw-bold">@Model.TotalAmount.ToString("C")</dd>
                    
                    <dt class="col-sm-4 text-success">المدفوع</dt>
                    <dd class="col-sm-8 text-success fw-bold">@Model.PaidAmount.ToString("C")</dd>

                    <dt class="col-sm-4 text-danger">المتبقي</dt>
                    <dd class="col-sm-8 text-danger fw-bold">@Model.RemainingAmount.ToString("C")</dd>
                </dl>
            </div>
        </div>
    </div>
</div>

<!-- Invoice Items Card -->
<div class="card shadow-sm mb-4">
    <div class="card-header"><h5 class="mb-0"><i class="bi bi-box-seam"></i> بنود الفاتورة</h5></div>
    <div class="card-body p-0">
        <table class="table table-striped table-hover mb-0">
            <thead class="table-light">
                <tr>
                    <th>الكود</th>
                    <th>المادة</th>
                    <th class="text-center">الكمية</th>
                    <th class="text-end">سعر الوحدة</th>
                    <th class="text-end">الإجمالي</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.MaterialCode</td>
                        <td>@item.MaterialName</td>
                        <td class="text-center">@item.Quantity</td>
                        <td class="text-end">@item.UnitPrice.ToString("C")</td>
                        <td class="text-end">@item.TotalPrice.ToString("C")</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- Payments History Card -->
<div class="card shadow-sm">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-send-check"></i> سجل الدفعات للمورد</h5>
        @if (Model.RemainingAmount > 0)
        {
            <a asp-controller="Payment" asp-action="AddSupplierPayment" asp-route-supplierId="@Model.SupplierId" asp-route-invoiceId="@Model.Id" class="btn btn-primary btn-sm">
                <i class="bi bi-plus-circle"></i> إضافة دفعة لهذه الفاتورة
            </a>
        }
    </div>
    <div class="card-body">
        @if (payments != null && payments.Any())
        {
            <table class="table table-sm">
                 <thead>
                    <tr>
                        <th>تاريخ الدفعة</th>
                        <th>المبلغ</th>
                        <th>طريقة الدفع</th>
                        <th>ملاحظات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var payment in payments)
                    {
                        <tr>
                            <td>@payment.PaymentDate.ToString("yyyy-MM-dd")</td>
                            <td>@payment.Amount.ToString("C")</td>
                            <td>@payment.PaymentMethod</td>
                            <td>@(payment.Notes ?? "---")</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p class="text-center text-muted">لا توجد دفعات مسجلة لهذه الفاتورة.</p>
        }
    </div>
</div> *@