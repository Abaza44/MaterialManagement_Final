@model MaterialManagement.BLL.ModelVM.Material.MaterialViewModel

@{
    ViewData["Title"] = "حذف مادة";
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">@TempData["ErrorMessage"]</div>
}
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}
<div class="erp-page erp-confirm-page">
    <div class="erp-page-header">
        <div>
            <p class="erp-eyebrow">المخزون</p>
            <h1 class="text-danger">حذف مادة</h1>
        </div>
    </div>

    <div class="erp-panel erp-danger-zone">
        <div class="erp-panel-header"><h2>تأكيد الحذف</h2></div>
        <p class="mb-3">هل أنت متأكد أنك تريد حذف المادة <strong>@Model.Name</strong>؟</p>

        <div class="erp-detail-grid mb-3">
            <div class="erp-detail-item"><span>الكود</span><strong>@Model.Code</strong></div>
            <div class="erp-detail-item"><span>الكمية</span><strong>@Model.Quantity.ToString("N2")</strong></div>
        </div>

        <form asp-action="Delete" method="post" class="erp-form-actions">
            <input type="hidden" asp-for="Id" />
            <partial name="_SupervisorPasswordField" />
            <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> حذف</button>
            <a asp-action="Index" class="btn btn-outline-secondary">رجوع</a>
        </form>
    </div>
</div>
