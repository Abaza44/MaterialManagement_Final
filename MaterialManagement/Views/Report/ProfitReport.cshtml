@{
    ViewData["Title"] = "تقرير الأرباح";
}

<div class="erp-page erp-report-picker-page erp-report-page">
    <div class="erp-page-header">
        <div>
            <p class="erp-eyebrow">التقارير</p>
            <h1>تقرير الأرباح</h1>
        </div>
    </div>

<div class="erp-panel erp-report-filter-panel">
    <div class="erp-panel-header">
        <div>
            <h2>اختر الفترة الزمنية</h2>
            <p class="erp-table-note">يعرض التقرير صافي البيع والتكلفة والربح التقريبي حسب منطق التقارير الحالي.</p>
        </div>
    </div>
        <form asp-action="ProfitReport" method="post">
            <div class="erp-form-grid">
                <div>
                    <label class="form-label fw-bold">من تاريخ</label>
                    <input type="date" name="fromDate" class="form-control" required value="@DateTime.Now.ToString("yyyy-MM-dd")" />
                </div>
                 <div>
                    <label class="form-label fw-bold">إلى تاريخ</label>
                    <input type="date" name="toDate" class="form-control" required value="@DateTime.Now.ToString("yyyy-MM-dd")" />
                </div>
            </div>
            <div class="erp-form-actions">
                <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i> عرض تقرير الأرباح</button>
            </div>
        </form>
</div>
</div>
